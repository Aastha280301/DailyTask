<h1>student Feedback Form</h1>

<ul>
  <% @students.each do |student| %>
    <li>
      <%= link_to student.registration_id, student %>
      <%= link_to student.roll_no, student %>
       <%= link_to student.student_name, student %>
    </li>
  <% end %>
</ul>

<%= link_to "New student", new_student_path %>